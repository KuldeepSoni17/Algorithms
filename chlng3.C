#include<stdio.h>
#include<conio.h>
#include<string.h>

main()
{
    int L,i,sum=0;
    char str1[100];
    gets(str1);
    L = strlen(str1);
    if(str1[0] == str1[L-1])
    {
        sum = 1;
    }
    for(i = L-1 ;i<1;i--)
    {
    sum = ;
    }
printf("%d",sum);

}
